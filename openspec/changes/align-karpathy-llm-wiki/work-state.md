# Implementation checkpoint

Status: **in progress, not release-approved**. Source of truth is the current worktree and fresh command output; this checkpoint is not completion evidence.

- Branch: `codex/deepseek-harness-plugin`; starting commit: `172d0e5`.
- Implemented draft: six page purposes, rules, catalog-first compilation/query, explicit filing/apply, structural/semantic lint, dependency-aware permissions, encrypted persistence, legacy-memory navigation, public catalog, and CLI/MCP/Harness entry points.
- Current runtime/test candidate: `a71e3a71572a701d82e036dac1e72402e26f61e7`. Last complete suite was `21fc23b`: macOS arm64, Node 22.17.0, 266/266, zero failures/skips, 27,816.635792 ms. After subsequent fixes, governance/legacy/erasure/operation-permission tests passed 19/19; expanded governance passed 7/7; strict build/test typing and combined schema/crash/sync suite passed 131/131, zero skips, 36,182.879750 ms. The complete current suite and environment matrix remain to run.
- Early audit fixes already written: body links cannot bypass explicit dependencies; catalog metadata participates in output restrictions; doctor/sync inspect Wiki provenance; lint retains structural results on model failure; dependent-page changes are included in the reviewable atomic plan.
- Implemented and locally committed: authenticated query results/plans/receipts, idempotent filing, encrypted Wiki erasure, adapter workflows. The isolated review of `af8181b` returned nine confirmed issues and one exported-search inconsistency. Re-review of `21fc23b` closed those issues (146/146 reviewer tests) but found public legacy-supported Wiki pages missing from WIKI.md. Commit `80c455d` fixes this and a newly reproduced hidden-target overwrite authorization defect. Commit `91075e9` fixes swallowed, potentially content-echoing malformed YAML errors; `a71e3a7` adds actual subprocess crash, concurrency and local-remote sync evidence. Current isolated acceptance review is pending.
- Fixed review boundaries: expiry/config revalidation during provider work and before sealing, source-identity attribution, duplicate provider targets, actual atomic dependency revisions, full CommonMark link validation, public catalog diagnostics, built-in rule version provenance, lint page versions and all-or-nothing semantic suggestions, consistent standalone search. Unconsulted raw capture no longer invalidates a completed ingest.
- Installed-package gate passed on `21fc23b` runtime with the new parser and user guide (84 entries): independent API/CLI/MCP/Harness compile, query, filing, repair, persistent readback, type declarations and denial. Production audit found zero vulnerabilities; strict OpenSpec passed 8/8. Re-run gates for the final current candidate; these are not hosted CI or live-model quality evidence.
- Active work: package/docs commit, final exact-candidate independent review, additional matrix evidence for schema, migration, concurrency, lifecycle, cancellation, multi-page crash recovery and remote synchronization.
- Still required: map all 44 acceptance criteria to executed evidence; update user documentation; all retained/new tests; supported Node/Git-default/platform matrix; production dependency audit; strict OpenSpec and package gates; split local commits; isolated read-only review of the exact committed candidate and repairs until no confirmed issue remains.
- Local commits: `3daa871` provider; `084a6b4` Wiki core; `af8181b` adapters; `21fc23b` review fixes; `80c455d` legacy/authority; `91075e9` managed Markdown validation; `a71e3a7` recovery tests. No push/publication authorized by this goal; no final independent approval yet.

Environment: use explicit working directory `/Users/imac/code/memobranch` because the original task directory was removed. Supported local verification runtime is Node 22.17.0. Write approvals briefly failed for exhausted workspace credits and later recovered; do not route around a rejected approval.
